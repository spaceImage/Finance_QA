# 오늘 작업 발표 스크립트

*[modify.md](modify.md)의 1~10번 섹션 순서를 그대로 따라간 발표용 스크립트입니다. 비전공자도 이해할 수 있도록 소리 내어 읽기 좋게 풀어썼습니다.*

---

## 파트 1 — 이 프로젝트는 뭘 하는 프로그램인가

먼저 이 프로젝트가 뭘 하는 건지부터 말씀드릴게요. **보험약관 PDF, 수백 페이지짜리를 AI한테 미리 읽혀두고, '내가 이런 경우 보험금 얼마 받을 수 있어?' 하고 물어보면 정확한 근거(몇 조, 몇 페이지)를 대면서 답해주는 챗봇**을 만들고 있습니다.

크게 두 단계로 나뉘는데요, 사람 한 명당 약관 PDF랑 보험증권을 넣으면 AI가 검색할 수 있는 형태로 미리 변환해두는 '준비 단계'가 있고, 그다음 사용자가 실제로 질문하면 그 데이터에서 관련 조항을 찾아와 답변을 만드는 '질문 단계'가 있습니다.

---

## 파트 2 — 파일별 역할 (전체 지도)

프로젝트가 어떤 파일들로 구성돼 있는지 간단히 짚고 갈게요.

준비 단계는 3단계 파이프라인으로 되어 있습니다. PDF를 목차 기준으로 잘라서 JSON으로 만드는 1단계, 그걸 한 사람 기준으로 합치는 2단계, 그걸 페이지 단위로 잘라서 AI가 검색할 수 있는 벡터로 바꿔 클라우드 DB에 저장하는 3단계까지요. 이 전체를 메뉴 하나로 실행할 수 있는 진입점 파일도 있고, 여러 파일이 공통으로 쓰는 부품들을 모아둔 파일도 있습니다.

질문 단계는 실제로 질문에 답하는 엔진 파일 하나랑, 정확도를 검증하는 테스트 스크립트 하나로 구성돼 있고요.

그리고 사람별 데이터는 `tasks/이름` 폴더 밑에, 그 사람의 원본 PDF, 파싱된 결과물, 보험증권 요약, 그리고 이번에 새로 추가한 '실제 가입한 특약 목록' 파일까지 들어있습니다.

---

## 파트 3 — 시스템 아키텍처 다이어그램

구조를 그림으로 두 개 그려뒀습니다.

하나는 **지금 실제로 구현된 것**이에요. 약관 PDF가 3단계를 거쳐 벡터DB에 들어가는 준비 과정, 그리고 질문이 들어오면 관련 특약을 분류하고, 검색하고, 검색 결과를 평가하고, 답변을 만드는 전체 흐름을 그려뒀습니다.

다른 하나는 **목표 아키텍처**, 그러니까 앞으로 팀에서 만들려는 '상담사 보조도구'의 최종 그림입니다. 이건 아직 코드로 하나도 안 만들어져 있고요, 입력 검증하는 단계, 여러 질문을 나눠 처리하는 단계, 참조 조항을 따라가며 재검색하는 단계, 결과를 모아서 대시보드로 보여주는 단계까지 계획만 세워둔 상태입니다. 이 중에서 지금 스코프에서 뺀 부분('작업 분할', 'LLM 가드레일 실시간 검증')은 회색으로 표시해뒀습니다.

---

## 파트 4 — 오늘 발견하고 고친 문제들

여기서부터가 오늘 실제로 한 작업입니다. 지금까지 만들어둔 게 제대로 동작하는지 확인해봤더니 문제가 4개 나왔어요.

**첫 번째, 가장 심각했던 문제**는 약관 본문이 사실상 통째로 사라지고 있었다는 겁니다. 296페이지짜리 약관을 저장해뒀는데 열어보니 실제 조항 내용은 하나도 없고 페이지 번호만 들어있었어요. PDF에서 글자를 뽑아오는 코드가 한 페이지에서 딱 한 문단만 남기고 나머지를 다 버리는 실수를 하고 있었는데, 하필 그 남은 한 문단이 페이지 하단 쪽번호였던 거예요. 이걸 고쳐서 296페이지 전부 실제 내용이 들어가게 만들었습니다.

**두 번째**, 맨 마지막 특약의 페이지 수 계산이 틀려있었어요. PDF 전체 페이지 수를 그대로 약관 페이지 번호로 착각한 계산 실수였는데, 다행히 실제 내용 손실은 없었고 숫자만 틀려있던 거라 바로 잡았습니다.

**세 번째**, 벡터DB에 옛날 데이터와 새 데이터가 뒤섞여서 쌓이고 있었어요. 다시 만들 때마다 기존 걸 안 지우고 계속 추가만 하는 구조였거든요. 이제는 다시 만들 때 그 사람 기존 데이터를 먼저 지우고 새로 넣도록 고쳤습니다.

**네 번째**는 나중에 서버에 배포할 걸 대비한 선제 조치들이에요. 파일 이름이 대소문자 문제로 서버에서 깨질 수 있는 부분을 미리 고쳤고, 특정 인물 이름이 코드에 박혀있던 걸 일반화했고, 가장 크게는 로컬에 저장하던 벡터DB를 클라우드(Supabase)로 옮겼습니다. 서버에 배포하면 로컬 파일이 사라지기 때문에 꼭 필요한 작업이었어요.

---

## 파트 5 — RAG 방식을 3개 → 1개로 정리

답변을 만드는 방식이 원래 3가지 버전으로 나뉘어 있었어요. 단순 텍스트로 답하는 버전, JSON으로 답하는 버전, 그리고 여러 단계를 거치는 정교한 버전이요. 정확도를 확인해본 뒤에 가장 정교한 버전 하나로 정리했습니다. 단순 버전은 아예 삭제했고, JSON으로 답하던 버전은 그 기능만 정교한 버전 안에 함수로 옮겨 담은 뒤에 삭제했습니다.

---

## 파트 6 — LangGraph는 어디서, 어떻게 쓰였나

지금 남은 유일한 답변 엔진 전체가 LangGraph라는 걸로 만들어져 있습니다. 왜 이게 필요했냐면, 단순한 방식은 질문을 그대로 검색해서 나온 걸 그대로 답변으로 써버리는데, 그러면 검색이 엉뚱한 데서 결과를 가져와도 그냥 그걸로 답변을 만들어버리는 문제가 있어요.

지금 쓰는 방식은 질문을 보고 관련 특약을 먼저 골라내고, 그 범위 안에서 검색하고, 검색된 문서 하나하나가 진짜 관련 있는지 다시 평가하고, 관련 있는 게 부족하면 질문을 스스로 다시 써서 재검색까지 하는 다단계 구조입니다. 그래도 안 나오면 못 찾았다고 솔직하게 안내하고요. 이렇게 판단하면서 되돌아갈 수 있는 구조라 훨씬 신뢰도가 높습니다.

---

## 파트 7 — 벤치마크/실사용 테스트로 찾은 문제들

이 답변 엔진이 진짜 정확한지 시험을 봤습니다. 정답을 미리 알고 있는 질문 10개를 준비해서 돌려보고, AI 답변이 실제 근거 문서에 기반했는지 또 다른 AI로 이중 검증까지 했어요.

결과가 4가지 나왔는데요:

- 첫 번째는 처음엔 오류라고 판단했다가, 다시 파보니 **오류가 아니었던 걸 정정**한 케이스입니다. 갑상선암 보장금액 질문에 AI가 30%라고 답했는데, 실제 약관을 다시 확인해보니 이게 맞는 답이었고, 검증하던 AI 판정관이 예외 케이스를 일반 케이스로 착각해서 잘못 채점했던 거였어요.
- 두 번째는 진짜 오류였습니다. 고객이 가입하지도 않은 특약을 AI가 가입했다고 착각해서 보험금을 두 배로 부풀려 답한 거예요. 원인을 찾아보니 AI가 후보로 고려하는 특약 목록에 이 고객이 실제로 안 산 특약까지 다 섞여 있었더라고요. 그래서 이 고객이 진짜 가입한 특약 목록만 따로 만들어서, 애초에 후보에도 못 들어오게 막았습니다.
- 세 번째도 진짜 오류였습니다. 통원 치료비를 계산할 때 근거 문서에도 없는 90%라는 숫자를 AI가 그냥 상식적으로 갖다 붙인 거였어요. 프롬프트에 '문서에 실제로 적힌 숫자만 써라'는 규칙을 추가해서 고쳤습니다.
- 네 번째는, 고객이 가입 안 한 보장을 물어봤을 때 AI가 '보험사에 직접 문의하세요'라고 답하던 문제입니다. 이 챗봇 자체가 상담원 역할인데 사용자를 다른 데로 떠넘기는 것처럼 들리니까요. 지금은 '가입 목록에 없어서 보험금을 받으실 수 없습니다'라고 확실하게 답하도록 바꿨습니다.

---

## 파트 8 — 응답 속도 개선

질문에 따라 최대 27초까지 걸리던 게 있었는데, 원인을 나눠서 손봤어요. 검색된 문서를 하나씩 순서대로 AI한테 물어보던 걸 한꺼번에 동시에 물어보도록 바꿔서 27초가 7~9초로 줄었고요, 답변을 다 만든 다음에 한 번에 보여주는 대신 글자가 만들어지는 대로 실시간으로 흘러나오게 만들어서, 실제 계산 시간은 같아도 훨씬 안 기다리는 느낌을 주게 했습니다. 덤으로 터미널에서 직접 원하는 질문을 자유롭게 입력해볼 수 있게도 만들었어요.

---

## 파트 9 — Supabase DB 구조 정리

클라우드 데이터베이스를 열어봤더니, 저희가 안 쓰는 테이블 3개(회원, 상품, 가입계약)가 이미 만들어져 있었어요. 코드 어디서도 쓰지 않는 테이블이었고, 지금 프로젝트 규모에 안 맞는다고 판단해서 정리했습니다. 지금은 벡터DB 테이블 하나만 깔끔하게 남아있습니다.

---

## 파트 10 — 배포 전에 꼭 확인할 것

마지막으로 체크리스트인데요, 나중에 서버에 배포할 때 API 키들을 서버 환경변수에 따로 넣어줘야 한다는 것, 그리고 새로운 사람을 추가하고 싶으면 폴더에 PDF랑 목차표만 넣고 파이프라인 돌리면 끝난다는 것, 이 두 가지를 남겨뒀습니다.
