# 발표 슬라이드 구성안 — Finance QA: 보험약관 Agentic RAG 상담 지원 시스템

> 총 발표 시간: **15분** (슬라이드 15장, 장당 평균 1분 내외)
> 구성: **기(도입 3분) → 승(전개 5분) → 전(전환/트러블슈팅 4분) → 결(마무리 3분)**
> 이 파일은 실제 PPT(파워포인트/구글슬라이드/Keynote) 제작 전 콘텐츠 초안입니다. 슬라이드별로 "제목 / 화면에 넣을 내용 / 발표 멘트"를 분리해뒀으니, 그대로 슬라이드에 옮기고 발표 멘트는 큐카드로 활용하시면 됩니다.

---

## ⏱️ 전체 타임라인

| 구간 | 슬라이드 | 시간 | 핵심 메시지 |
|---|---|---|---|
| 기 (도입) | 1~3 | 3분 | 왜 이 프로젝트가 필요한가 |
| 승 (전개) | 4~8 | 5분 | 무엇을, 어떻게 만들었나 |
| 전 (전환) | 9~11 | 4분 | 만들면서 부딪힌 문제와 해결 |
| 결 (마무리) | 12~15 | 3분 | 결과, 데모, 남은 과제 |

---

## 🟦 기(起) — 도입 (3분)

### Slide 1. 표지
**화면 내용**
- 타이틀: **Finance QA — 보험약관 Agentic RAG 상담 지원 시스템**
- 서브타이틀: AIQ CS/CX 인바운드 상담사를 위한 AI 손해사정 보조 시스템
- 발표자 / 팀명 / 날짜

**발표 멘트**
- "안녕하세요, 오늘 소개드릴 프로젝트는 보험약관을 읽고 보상금을 정확히 계산해주는 AI 상담 지원 시스템입니다."

---

### Slide 2. 문제 정의 (Pain Point)
**화면 내용**
- 보험 약관 1건 = 최대 **300페이지**, 특약 **32종**
- 상담사가 전화 중 실시간으로 "이 특약 보장되나요? 얼마 받나요?"에 정확히 답하기 어려움
- 잘못된 안내 → 고객 불만/재문의, 정확한 계산 → 시간 소요
- 기존 단순 RAG(질문→검색→답변)의 한계: 엉뚱한 특약 혼입, 근거 없는 수치 할루시네이션

**발표 멘트**
- "실제 상담 현장에서는 300페이지짜리 약관과 32개 특약 중 어떤 게 이 고객에게 해당하는지, 그리고 실제 금액이 얼마인지를 몇 초 안에 답해야 합니다. 이걸 사람이 매번 조항을 찾아 계산하는 건 현실적으로 어렵고, 그렇다고 단순 챗봇에 맡기면 안 가입한 특약을 근거로 쓰거나 숫자를 지어내는 문제가 생깁니다."

---

### Slide 3. 프로젝트 목표
**화면 내용**
- 목표: 약관 PDF → 구조화 데이터 → **Agentic RAG**로 "가입 여부 확인 → 약관 대조 → 보상금 계산"까지 자동화
- 3대 원칙
  1. **근거 기반**: 실제 약관 조항·가입 증권 수치만으로 답변 (추측 금지)
  2. **개인화**: 이 사람이 실제 가입한 특약만 근거로 사용
  3. **실시간성**: 상담 중 바로 쓸 수 있는 스트리밍 응답

**발표 멘트**
- "저희는 단순히 '검색해서 답하는' 챗봇이 아니라, 스스로 작업을 계획하고 필요하면 되묻고 재검색까지 하는 Agentic RAG를 목표로 했습니다."

---

## 🟩 승(承) — 전개 (5분)

### Slide 4. 전체 시스템 아키텍처
**화면 내용** (다이어그램)
```
PDF 약관 ─▶ 파싱(step_1) ─▶ 병합(step_2) ─▶ 임베딩·적재(step_3)
                                                      │
                                                      ▼
                                          Supabase (pgvector) Vector DB
                                                      │
질문 ─▶ FastAPI SSE 서버 ─▶ LangGraph Agentic RAG 엔진 ─▶ 실시간 스트리밍 답변
                │                                              │
                ▼                                              ▼
        Next.js 상담사 화면(AIQ 채팅/문서 뷰어)      디버그 콘솔(/debug)
```
**발표 멘트**
- "크게 4단계입니다. 약관 PDF를 구조화해 벡터DB에 넣는 데이터 파이프라인, 질문을 처리하는 LangGraph 엔진, 이를 실시간 스트리밍으로 서빙하는 FastAPI 서버, 그리고 상담사가 실제로 보는 Next.js 화면입니다."

---

### Slide 5. 데이터 파이프라인 (PDF → Vector DB)
**화면 내용**
- **1단계 (step_1.py)**: PyMuPDF로 PDF 파싱, `toc_config.csv` 기준 특약별 분할, 2단/단일 컬럼 레이아웃 자동 판별
- **2단계 (step_2.py)**: 특약별 JSON → 인물 1명당 최종 통합 JSON
- **3단계 (step_3.py)**: 페이지 단위 청크 + 400자 오버랩 → OpenAI `text-embedding-3-small` 임베딩 → Supabase 적재
- 재실행 시 기존 벡터 자동 삭제 후 재적재 (idempotent) → 중복 누적 방지
- 여러 고객 데이터를 한 테이블에서 `task_name`으로 격리 → 서버리스 배포에도 안전

**발표 멘트**
- "약관은 신문처럼 2단으로 인쇄된 페이지도 있어서, 그냥 텍스트를 뽑으면 좌우 문단이 섞입니다. 레이아웃을 자동 판별해서 읽는 순서대로 정렬하는 게 이 단계의 핵심입니다."

---

### Slide 6. LangGraph Agentic Workflow (핵심 엔진)
**화면 내용** (노드 플로우 다이어그램)
```
질문 검증 → 작업 자율 기획(Task Planner) → 의도 라우팅(가입 특약만 후보로)
   → 슬롯 체크(정보 부족 시 되묻기) → 병렬 증권·약관 검색(Fan-out/Fan-in)
   → 문서 관련성 평가 → (부족하면 질문 재작성 후 재검색)
   → 연쇄 참조 조항 추적(Multi-hop) → 최종 답변 + UI Block 생성
```
- 11개 노드, 조건부 분기 + 반복 루프(Reflection Loop)를 가진 상태 기계(State Graph)
- 범위 밖 질문(타사 비교, 날씨 등)은 검색 없이 즉시 차단

**발표 멘트**
- "이 워크플로우가 프로젝트의 핵심입니다. 질문을 그대로 검색에 던지지 않고, 먼저 '이 질문에 답하려면 어떤 작업이 필요한지' 스스로 계획을 세우고, 실제 가입한 특약으로 검색 범위를 좁히고, 검색 결과가 부족하면 스스로 질문을 바꿔 재검색합니다."

---

### Slide 7. Model Orchestration 전략
**화면 내용** (표)
| 역할 | 모델 | 이유 |
|---|---|---|
| 분류/라우팅/검증 (6개 노드) | GPT-4o mini | 반복 호출 많음 → 속도·비용 우선 |
| 최종 답변 생성 (1개 노드) | GPT-5 mini | 긴 문맥 추론 + 정확한 계산 필요 |

- `config.py`에서 노드별 모델을 환경변수로 관리 (하드코딩 X)
- "분류는 작은 모델, 추론/생성은 큰 모델" 원칙

**발표 멘트**
- "모든 단계에 같은 모델을 쓰면 느리고 비쌉니다. 저희는 반복이 많은 분류·검증 작업은 가벼운 모델로, 실제 보상금을 계산하고 문장을 만드는 마지막 단계만 고성능 모델로 나눠서 응답 속도와 비용을 최적화했습니다."

---

### Slide 8. 프론트엔드 — 상담사 화면 & 디버그 콘솔
**화면 내용** (스크린샷 또는 목업)
- **메인 화면**: 좌측 고객/가입상품 정보, 우측 AIQ 채팅 + 문서(약관 PDF/증권) 탭
- 답변은 4종 UI Block(CONTEXT·RETRIEVAL_RESULT·CAUTION·DELIVER)으로 구조화되어 렌더링, 근거 조항 클릭 시 원문 스니펫 모달
- **디버그 콘솔(`/debug`)**: 프리셋 질의 실행, 노드별 소요시간/사용 모델/Raw JSON 실시간 확인
- SSE로 노드 진행 상황 + 글자 단위 타이핑 애니메이션 스트리밍

**발표 멘트**
- "상담사는 이 화면에서 질문만 입력하면 됩니다. 답변이 '어떤 근거로 나왔는지'가 항상 구조화된 블록으로 같이 보이고, 원문 조항을 클릭하면 실제 PDF 페이지가 하이라이트됩니다."

---

## 🟧 전(轉) — 전환 / 트러블슈팅 (4분)

### Slide 9. 문제 발견 ① — 데이터 자체가 비어있었다
**화면 내용**
- 초기 파싱 로직 버그: 페이지 내 여러 문단 중 **첫 블록만 남기고 나머지 폐기** → 실제로는 페이지 하단 쪽번호만 저장됨
- 296페이지 벡터DB가 사실상 "빈 껍데기"
- 조치: 블록 필터링 로직 수정 + 2단 레이아웃 자동 정렬 → 296페이지 전량 본문 정상 확인
- 부가 문제: 재구축 시 기존 벡터 삭제 없이 계속 추가 → 신구 데이터 혼재(2배 적재) → idempotent 재적재로 해결

**발표 멘트**
- "가장 뼈아팠던 문제입니다. 검색 자체는 잘 되는데 알고 보니 벡터DB에 실제 조항 내용이 없고 페이지 번호만 들어있었어요. 파싱 코드가 문단을 잘못 필터링하고 있었던 거죠. 이건 결과물이 그럴싸해 보여도 실제로는 아무 근거가 없었던, 발견하기 가장 어려운 유형의 버그였습니다."

---

### Slide 10. 문제 발견 ② — 정확도: 안 가입한 특약 혼입 & 할루시네이션
**화면 내용**
- **사례 A**: "암 치료 10일 입원 시 입원비?" → 가입하지 않은 "암치료비특약"까지 근거로 섞여 보상금 **2배 부풀림**
  → 원인: 라우터가 상품 전체 32개 특약을 후보로 삼음
  → 해결: `enrolled_sections.json`(실제 가입 특약 12개) allowlist로 후보 사전 필터링
- **사례 B**: 실손 통원 보상 계산에 근거 문서에 없는 "90%"를 LLM이 임의로 갖다 붙임(순수 할루시네이션)
  → 해결: "계산 비율은 반드시 검색된 조항에 실제로 적힌 숫자만 사용" 프롬프트 규칙 추가
- **사례 C**: 가입하지 않은 보장을 물어보면 "보험사에 문의하세요"로 얼버무림 → 확정적 안내("보상 불가")로 프롬프트 수정

**발표 멘트**
- "정확도 문제는 크게 두 갈래였습니다. 하나는 안 가입한 특약이 근거로 섞이는 것, 다른 하나는 모델이 그럴듯한 일반 상식(90%)을 실제 조항인 것처럼 갖다 쓰는 할루시네이션이었습니다. 둘 다 벤치마크로 잡아내서 각각 allowlist 필터링과 프롬프트 규칙으로 해결했습니다."

---

### Slide 11. 성능 개선 — 27초에서 7~9초로
**화면 내용**
- 문서 검색이 많은 질문은 최대 **27초** 소요
- **원인 ①**: 문서 관련성 평가를 한 개씩 순차 LLM 호출 → **배치(batch) 병렬 호출**로 전환 → 계산 시간 7~9초대로 단축
- **원인 ②(체감)**: 답변 완성까지 무응답 대기 → **SSE 글자 단위 스트리밍**으로 체감 대기시간 감소
- **추가**: 후속 질문(맥락 연속 질의)은 전체 그래프 대신 **1.5초 패스트트랙**으로 응답

**발표 멘트**
- "실제 계산 시간과 체감 대기시간을 나눠서 접근했습니다. 문서 평가를 병렬화해서 실제 계산 시간을 줄이고, 스트리밍으로 기다리는 동안에도 화면에 뭔가 보이게 해서 체감 대기시간까지 줄였습니다."

---

## 🟥 결(結) — 마무리 (3분)

### Slide 12. 검증 — 벤치마크 결과
**화면 내용**
- 검증 질문 **15종** (단일 질의 10 + Multi-hop 복합 질의 5)
- 답변을 실제 근거 문서·증권과 대조하는 **LLM Grounding 이중 검증** 파이프라인 (`benchmark.py`)
- PASS/FAIL + 판정 사유 + 소요시간을 `benchmark_results.json`으로 자동 기록 → 회귀 테스트로 재사용

**발표 멘트**
- "정답을 이미 아는 질문 15개를 만들어두고, 답변이 실제 근거에 기반했는지를 또 다른 LLM으로 검증하는 이중 체크 구조를 만들었습니다. 프롬프트나 로직을 바꿀 때마다 이걸 돌려서 정확도가 떨어지지 않았는지 확인합니다."

---

### Slide 13. 데모
**화면 내용** (라이브 데모 또는 스크린샷 시퀀스)
1. "재해골절 시 얼마 나오나요?" → 정상 RAG, 근거 조항과 함께 금액 계산
2. "장염으로 수액 맞았는데 10만원 나왔어요" → 실손 특약 조항 근거로 정밀 계산
3. "AIQ는 왜 보험료가 비싼가요?" → Out-of-Scope 즉시 차단
4. 디버그 콘솔에서 노드별 로그/모델/소요시간 실시간 확인

**발표 멘트**
- "실제로 몇 가지 질문을 던져보겠습니다. 정상 케이스, 계산이 필요한 케이스, 그리고 범위 밖 질문을 어떻게 걸러내는지 보여드리겠습니다."

---

### Slide 14. 한계 및 다음 단계
**화면 내용**
- 현재는 **단일 고객(장석찬)** 하드코딩 → 다중 고객 지원(API 연동) 필요
- 세션/피드백(👍👎)이 로컬 state에만 존재 → 서버 반영 필요
- 사용자 모드 토글 등 일부 UI는 아직 시각적 껍데기
- 향후: Vercel 배포, 다중 고객 온보딩 자동화, 상담사 피드백 학습 반영

**발표 멘트**
- "지금은 데모를 위해 한 명의 고객 데이터로 고정되어 있습니다. 다음 단계는 이걸 여러 고객으로 확장하고, 실제 배포 환경에서 상담사 피드백을 반영하는 구조를 만드는 것입니다."

---

### Slide 15. 마무리 & Q&A
**화면 내용**
- 핵심 요약 3줄
  1. 300페이지 약관 + 32개 특약 → 구조화된 벡터DB
  2. LangGraph Agentic Workflow로 "계획-검증-검색-추론-생성"까지 자율 수행
  3. 실측 벤치마크로 검증된 근거 기반 답변, 27초 → 7~9초로 속도 개선
- "감사합니다. 질문 받겠습니다."

**발표 멘트**
- "정리하면, 저희는 단순 검색 챗봇이 아니라 스스로 계획하고 검증하는 Agentic RAG로 상담사의 정확한 보상금 안내를 돕는 시스템을 만들었습니다. 질문 있으시면 편하게 해주세요."

---

## 🎤 발표 팁

- **9~11번 슬라이드(트러블슈팅)**가 이 발표의 차별점입니다. "그냥 만들었다"가 아니라 "만들고 검증하다 실제 버그를 찾아 고쳤다"는 서사가 신뢰도를 크게 높이므로 시간 배분을 아끼지 마세요.
- 라이브 데모(Slide 13)가 실패할 경우를 대비해 스크린 녹화본을 백업으로 준비하세요.
- Q&A에서 나올 법한 질문: "다른 보험사 약관에도 적용 가능한가?" (→ `toc_config.csv` + `task_name`만 바꾸면 확장 가능), "왜 LangGraph를 썼는가?" (→ 조건부 분기·병렬 실행·재시도 루프가 필요했기 때문).
