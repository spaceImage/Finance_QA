# 백엔드(FastAPI) 서버 실행에 필요한 패키지. 프로젝트 루트 requirements.txt(RAG 파이프라인용)와는 별도입니다.
# 설치: pip install -r backend/requirements.txt
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.7.0
